# Install as /etc/arena/health.env with mode 0640, root:arena ownership.
# Contains no tenant credentials. Both Supervisor and health checks read it.
ARENA_RUNTIME_DIR=/var/lib/arena
ARENA_DEBUG_PORT=8120
ARENA_MIN_FREE_BYTES=1073741824
