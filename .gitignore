# 密钥与环境配置（绝不入仓）
.env
.env.local
.env.*.local

# 运行时产物（遥测/manifest 含脱敏数据，仍不进 git）。
# 根锚定（/runtime/）防止误匹配 internal/runtime/ 源码包。
# 例外：模拟器平台化场景/策略资产（P1 固化，入库版本管理）。
# 注意：忽略内容而非目录本身（否则无法重新包含子目录）。
/runtime/*
!/runtime/scenes/
/runtime/scenes/*
!/runtime/scenes/*.json
!/runtime/policies/
/runtime/policies/*
!/runtime/policies/*.json
/runs/
/logs/
/alerts/
*.jsonl
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.db

# 构建产物
bin/
dist/

# 外部克隆/并行开发 worktree（独立分支/索引）
.worktrees/
.agents/

# 本地临时与 IDE
*.tmp
*.bak
.DS_Store
Thumbs.db
.idea/
.vscode/
