{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/DeliciousBuding/arena/contracts/race/race-result-v2.schema.json",
  "title": "Arena Race Result v2",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "raceId",
    "createdAt",
    "implementation",
    "git",
    "profile",
    "scenario",
    "submissionMode",
    "hardGates",
    "eligible",
    "metrics",
    "evidence"
  ],
  "properties": {
    "schemaVersion": { "const": "race.result.v2" },
    "raceId": { "type": "string", "minLength": 3, "maxLength": 128 },
    "createdAt": { "type": "string", "format": "date-time" },
    "implementation": { "enum": ["ts", "rust"] },
    "submissionMode": { "enum": ["offline", "shadow", "bounded-live", "live"] },
    "git": {
      "type": "object",
      "additionalProperties": false,
      "required": ["branch", "sha"],
      "properties": {
        "branch": { "type": "string", "minLength": 1 },
        "sha": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
        "buildId": { "type": ["string", "null"] }
      }
    },
    "profile": {
      "type": "object",
      "additionalProperties": false,
      "required": ["profileId", "schemaVersion", "hash", "unsupportedFields"],
      "properties": {
        "profileId": { "type": "string", "minLength": 1 },
        "schemaVersion": { "const": "race.strategy-profile.v1" },
        "hash": { "type": "string", "minLength": 8 },
        "unsupportedFields": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true
        }
      }
    },
    "scenario": {
      "type": "object",
      "additionalProperties": false,
      "required": ["scenarioId", "scenarioHash", "rulesHash", "seeds", "ticksPerSeed", "refillMode"],
      "properties": {
        "scenarioId": { "type": "string", "minLength": 1 },
        "scenarioHash": { "type": "string", "minLength": 8 },
        "rulesVersion": { "type": ["string", "null"] },
        "rulesHash": { "type": "string", "minLength": 8 },
        "refillMode": { "enum": ["none", "approximate", "server-observed", "server-secret"] },
        "seeds": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "integer" },
          "uniqueItems": true
        },
        "ticksPerSeed": { "type": "integer", "minimum": 1 }
      }
    },
    "hardGates": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "wrongTickSubmit",
        "duplicateSubmit",
        "staleCandidateExecuted",
        "illegalFinalPlan",
        "unknownRepair",
        "secondWriter",
        "crossTenantStateLeak",
        "orphanProcess",
        "unhandledPanic",
        "silentPlannerFallback",
        "protocolContractMismatch",
        "credentialLeak"
      ],
      "properties": {
        "wrongTickSubmit": { "$ref": "#/$defs/count" },
        "duplicateSubmit": { "$ref": "#/$defs/count" },
        "staleCandidateExecuted": { "$ref": "#/$defs/count" },
        "illegalFinalPlan": { "$ref": "#/$defs/count" },
        "unknownRepair": { "$ref": "#/$defs/count" },
        "secondWriter": { "$ref": "#/$defs/count" },
        "crossTenantStateLeak": { "$ref": "#/$defs/count" },
        "orphanProcess": { "$ref": "#/$defs/count" },
        "unhandledPanic": { "$ref": "#/$defs/count" },
        "silentPlannerFallback": { "$ref": "#/$defs/count" },
        "protocolContractMismatch": { "$ref": "#/$defs/count" },
        "credentialLeak": { "$ref": "#/$defs/count" }
      }
    },
    "eligible": { "type": "boolean" },
    "metrics": {
      "type": "object",
      "additionalProperties": false,
      "required": ["core", "economy", "exploration", "combat", "system"],
      "properties": {
        "core": { "$ref": "#/$defs/metricGroup" },
        "economy": { "$ref": "#/$defs/metricGroup" },
        "exploration": { "$ref": "#/$defs/metricGroup" },
        "combat": { "$ref": "#/$defs/metricGroup" },
        "system": { "$ref": "#/$defs/metricGroup" }
      }
    },
    "score": {
      "type": ["number", "null"],
      "minimum": 0,
      "maximum": 100
    },
    "evidence": {
      "type": "object",
      "additionalProperties": false,
      "required": ["manifest", "summary", "raw"],
      "properties": {
        "manifest": { "type": "string", "minLength": 1 },
        "summary": { "type": "string", "minLength": 1 },
        "raw": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        }
      }
    },
    "notes": { "type": "array", "items": { "type": "string" } }
  },
  "$defs": {
    "count": { "type": "integer", "minimum": 0 },
    "metricGroup": {
      "type": "object",
      "additionalProperties": false,
      "required": ["applicable", "values"],
      "properties": {
        "applicable": { "type": "boolean" },
        "values": {
          "type": "object",
          "additionalProperties": {
            "type": ["number", "integer", "null"]
          }
        }
      }
    }
  }
}
