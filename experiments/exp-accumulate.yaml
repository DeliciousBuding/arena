name: exp-accumulate
description: 四账号并行攒资源实验 — 巡逻半径对比（目标：攒 30 兑换注册码）
tenants:
  - api_key_index: 0
    name: t1
    strategy: balance
    params: {accumulate_target: 30, guard_resources: 20, guard_force: 4, explore_radius: 8}
  - api_key_index: 1
    name: t2
    strategy: balance
    params: {accumulate_target: 30, guard_resources: 20, guard_force: 4, explore_radius: 5}
  - api_key_index: 2
    name: t3
    strategy: balance
    params: {accumulate_target: 30, guard_resources: 20, guard_force: 4, explore_radius: 12}
  - api_key_index: 3
    name: t4
    strategy: balance
    params: {accumulate_target: 30, guard_resources: 20, guard_force: 4, explore_radius: 10}
