{
  "name": "@arena/arena-agent",
  "version": "0.1.0",
  "description": "Arena Hero 编排层（决策层）：游戏循环、共享地图、策略、遥测、supervisor。TS 版，直接嵌入 pi-coding-agent。",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "tsx --test \"test/*.test.ts\""
  },
  "dependencies": {
    "@arena/arena-hero-ts": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.83.0",
    "@types/node": "^24.0.0",
    "tsx": "^4.23.4",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
