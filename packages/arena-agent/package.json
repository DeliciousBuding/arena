{
  "name": "@arena/arena-agent",
  "version": "0.1.0",
  "description": "Arena Hero 编排层（决策层）：游戏循环、共享地图、策略、遥测、supervisor。TS 版，直接嵌入 pi-coding-agent。",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc --noEmit && node scripts/check-sim-isolation.mjs",
    "test": "tsx --test \"test/*.test.ts\"",
    "arena:doctor": "tsx src/cli/run-tenant.ts --doctor",
    "arena:shadow": "tsx src/cli/run-tenant.ts",
    "arena:live": "tsx src/cli/run-tenant.ts --live",
    "arena:sim": "tsx src/cli/run-sim.ts",
    "sim:isolation-check": "node scripts/check-sim-isolation.mjs",
    "sim:test": "tsx --test test/sim-isolation.test.ts"
  },
  "dependencies": {
    "@arena/arena-hero-ts": "*",
    "@earendil-works/pi-coding-agent": "0.83.0",
    "@earendil-works/pi-agent-core": "^0.83.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.23.4",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
